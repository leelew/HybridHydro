program: run.py
project: CLFS
name: bayes_sweep
method: bayes
metric:
  name: val_acc_mean
  goal: maximize
early_terminate:
  type: envelope
parameters:
  n_filters_factor:
    distribution: categorical
    values: [1, 2, 3]
  stats_hs:
    distribution: categorical
    values: [0, 1, 2]
  batch_size:
    distribution: categorical
    values: [16, 32]
  #learning_rate:
  #  distribution: log_uniform
  #  min: -11.5129
  #  max: -4.605
  #batch_size:
  #  distribution: categorical
  #  values: [2, 4]
  #filter_size:
  #  distribution: categorical
  #  values: [3, 5]
command:
  - ${env}
  - python3
  - ${program}
  #- "--wandb"
  - ${args}
